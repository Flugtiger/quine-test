// MR-1, MR-2, MR-3: Base class for all domain events

package quine.demo.model.shared;

/**
 * Abstract base class for all domain events in the system.
 * Domain events represent significant occurrences in the domain that other parts
 * of the system may be interested in.
 */
public abstract class DomainEvent {
    // Base class for domain events
}
